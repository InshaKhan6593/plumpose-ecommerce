# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
.playwright
playwright-report/
playwright-report/index.html

# next.js
/.next/
# A second build beside the first, for an A/B (NEXT_DIST_DIR, next.config.ts)
/.next-*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

public/media

# The client's film. The repository is public and the footage is unreleased,
# like seed-assets/ below. Rebuild it with scripts/encode-videos.sh.
public/video/

.env
seed-assets/
.screenshots/

# Playwright output
test-results/

# AGENTS.md is regenerated by `next dev` on each run, so it stays out of the
# source. CLAUDE.md is NOT generated — it carries the project briefing that
# every agent reads first, so it is committed.
AGENTS.md
